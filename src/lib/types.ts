export type NavLink = {
  href: string;
  label: string;
};

export type ModuleItem = {
  id: string;
  title: string;
  description: string;
  icon: string;
};

export type Benefit = {
  title: string;
  description: string;
  icon: string;
};

export type StatItem = {
  value: string;
  label: string;
};

export type ContactFormData = {
  name: string;
  email: string;
  company: string;
  message: string;
};
